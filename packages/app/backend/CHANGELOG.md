# Changelog

## 0.1.0

Initial release. Sessions, users, roles, permissions (6 migrations). Login/signup/logout handlers and pipes. Auth status endpoint (`/api/auth/me`). CSRF protection with token rotation. CORS handling. Event system with login listener. Seeders for admin user and admin role.
