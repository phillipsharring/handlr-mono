# Changelog

## 0.16.0

Lockstep bump. No functional changes to `@phillipsharring/create-handlr-static` this release.

## 0.7.0 – 0.15.1

Co-versioned in lockstep with the framework since the monorepo formed. Notable in this
range: **0.7.1** fixed a stale import in the bundled static skeleton. Otherwise version
bumps only.

## 0.1.0

Initial release. Minimal CLI that copies the bundled static skeleton into a new project directory and runs `npm install`.
