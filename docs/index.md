---
layout: home

hero:
  name: Handlr
  text: Pipe + Handler for PHP
  tagline: A lightweight PHP framework with clean separation of HTTP and business logic — plus an HTMX + Handlebars frontend toolkit.
  actions:
    - theme: brand
      text: Get Started
      link: /getting-started/installation
    - theme: alt
      text: Backend
      link: /backend/
    - theme: alt
      text: Frontend
      link: /frontend/

features:
  - title: Pipe + Handler
    details: HTTP concerns live in Pipes. Business logic lives in Handlers with typed input and structured results. The same Handler serves HTTP requests and event dispatch.
  - title: Explicit, not magic
    details: Domain-driven, no static state, constructor injection everywhere. Clean abstractions you can read top to bottom — not a framework pretending to be Laravel.
  - title: Server-driven frontend
    details: handlr-build compiles static HTML. handlr-frontend adds HTMX, Handlebars, modals, CSRF, and auth-gated UI — so you focus on pages and domain logic.
---
