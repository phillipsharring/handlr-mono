# Runtime (handlr-frontend)

> [!NOTE] Draft
> This page is a stub. Content coming.

The runtime is a composable HTMX toolkit — import only what you need; `index.js` stays side-effect-free.

## To cover

- Initialization and opt-in wiring
- HTMX extensions: json-enc, client-side-templates
- Handlebars rendering
- `initModules()` and runtime module init
- Boosted navigation handling
