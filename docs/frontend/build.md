# Build (handlr-build)

> [!NOTE] Draft
> This page is a stub. Content coming.

## To cover

- Vite plugin + HTML compiler
- Layouts, pages, and `<component>` tags
- The `[[app]]` layout slot
- Tailwind integration and `@source` scanning
- Module page/component discovery (`resolveModuleDirs`)
- Building for production
