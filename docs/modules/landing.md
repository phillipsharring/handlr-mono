# Landing Module (email capture)

> [!NOTE] Draft
> This page is a stub. Content coming.

`handlr-module-landing` — a "coming soon" email capture page with an admin list.

- npm: `@phillipsharring/handlr-module-landing`
- Packagist: `phillipsharring/handlr-module-landing`

## To cover

- What it provides: `/landing/` page, admin captures list, `email_captures` table
- Frontend wiring (`site.config.js`, Tailwind `@source`)
- Backend wiring (`LandingServiceProvider`, migration)
- Configuration options (e.g. `adminNav`)
