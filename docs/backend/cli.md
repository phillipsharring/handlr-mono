# CLI & Makers

> [!NOTE] Draft
> This page is a stub. Content coming.

## To cover

- The `make` command and `GeneratorRunner`
- Built-in makers: Handler, Pipe, Record, Table, Migration, Seeder, Maker (and the ScaffoldMaker)
- Stubs and how generated files are placed
- Writing a custom `MakerInterface`
- `composer run migrate` / `migrate:rollback`
